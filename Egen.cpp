#include <sstream>
#include <iostream>

using namespace std;

#include "Egen.h"

void Egen::Print(string type, string prof, string title, string num) {
    cout << type << "-" << prof << "-" << title << "-" << num << endl;
}
